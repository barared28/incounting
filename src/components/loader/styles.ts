import { createStyles } from "@mantine/core";

export const useStylesLazy = createStyles((theme) => ({
  container: {
    width: "100vw",
    height: "100vh",
  },
}));

export const useStylesLoader = createStyles((theme) => ({
  container: {
    width: "100vw",
    height: "100vh",
  },
}));
