import { createStyles, rem } from "@mantine/core";

export const useStylesLang = createStyles((theme) => ({
  logo: {
    width: rem("20px"),
    height: rem("20px"),
  },
  select: {
    width: rem("80px"),
  },
}));
